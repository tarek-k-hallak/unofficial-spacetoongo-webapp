import { FooterContainer, HeaderContainer, FormContainer } from '../containers'

export default function Login() {
    return (
        <div>
            <HeaderContainer />
            <FormContainer />
            <FooterContainer />
        </div>
    )
}
